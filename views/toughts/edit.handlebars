<div class="tought-form-container">
  <a href="/toughts/dashboard">Voltar</a>
  <h1>Editando um Pensamento:</h1>
  <form action="/toughts/edit" method="POST">
    <input type="hidden" name="id" value={{tought.id}}>
    <div class="form-control">
        <label for="title">Título:</label>
        <input type="text" name="title" placeholder="Digite o título" value="{{tought.title}}">
    </div>
    <div class="form-control">
        <label for="title">Idade:</label>
        <input type="text" name="idade" placeholder="Digite o título" value="{{tought.idade}}">
    </div>
    <input type="submit" value="Editar Pensamento">
  </form>
</div>